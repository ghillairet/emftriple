@namespace(uri="http://eclipselabs.org/emftriple/model", prefix="model")
package model;

class Parent {
  id attr String ~id;
  attr String name;
  val Child[*] children;
}

class Child {
  id attr String ~id;
  attr String name;
}

