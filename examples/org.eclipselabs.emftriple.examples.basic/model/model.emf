@namespace(uri="http://eclipselabs.org/emftriple/model", prefix="model")
package model;

class Parent {
  id attr String ~id;
  attr String name;
  ref Child[*] children;
}

class Child {
  id attr String ~id;
  attr String name;
}

